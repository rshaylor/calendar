services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: familyhub:latest
    container_name: familyhub
    restart: unless-stopped
    ports:
      - "80:8000"
    volumes:
      - app_data:/data
    env_file:
      - apps/api/.env

volumes:
  app_data:
